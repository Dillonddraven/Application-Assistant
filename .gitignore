# --- USER DATA (never commit) ---
# Profile + secrets -- your real career history and PII
profile/master_profile.yaml
profile/secrets.yaml
profile/companies.yaml
profile/skill_synonyms.yaml

# Job postings + analyzed + tailored packets
jobs/raw_posts/
jobs/analyzed/
outputs/

# Tracker spreadsheet + dashboard reports
applications.xlsx
MORNING_REVIEW.md
RANKED_TODAY.md

# Multi-user web app per-user data (passwords + each user's profile + outputs)
users/

# One-off driver scripts + run logs (often contain real URLs / data)
runs/

# Finder review queue
runs/_finder_queue.xlsx

# --- ENV / SECRETS ---
.env
.env.local
*.env
!.env.example

# --- PYTHON ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# --- TOOLING CACHES ---
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- OS ---
.DS_Store
Thumbs.db
